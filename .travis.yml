# https://travis-ci.org

language: objective-c
os: osx
osx_image: xcode11.3

before_install:
  - brew update
  - brew install ldid dpkg xz
  - export THEOS=theos
  - sudo git clone --recursive https://github.com/theos/theos.git $THEOS
  - for i in `ls ${THEOS}/vendor/include/Preferences`; do echo "#import <Preferences/$i>" > ${THEOS}/vendor/include/Preferences/Preferences.h; done

script:
  - make all
